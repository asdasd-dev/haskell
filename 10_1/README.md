ѕусть мы представл€ем матрицы, как списки списков. Ќапример, список xss = [[1,2,3],[4,5,6],[7,8,9]] описывает матрицу 3 на 3, у который перва€ строка состоит из чисел 1 2 3 и т.д.

ќписать функцию isMagic, котора€ провер€ет, верно ли, что в данной матрице суммы всех строк, всех столбцов и главной и побочной диагонали равны между собой.

ѕримеры вызова:
isMagic [[2,7,6],[9,5,1],[4,3,8]] должно вернуть True.
isMagic [[1,2,3],[4,5,6],[7,8,9]] должно вернуть False.

«амечани€: 
- ћожно считать, что мы точно знаем, что данные описывают квадратную матрицу. “о есть, количество чисел в каждой строке равно количеству строк. Ёто можно не провер€ть.